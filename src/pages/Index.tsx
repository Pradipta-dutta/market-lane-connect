
// This file is replaced by the main App component
// Redirecting to maintain compatibility with existing routing
import App from '../App';

const Index = () => {
  return <App />;
};

export default Index;
